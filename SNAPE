#include <stdio.h>
#include <math.h>
 
int main(void)
{
  int t, b,ls; double min,max;
	 scanf("%d",&t);
	 while(t--)
	 {
	 	fflush(stdin);
	 	scanf("%d %d",&b,&ls);
	 	min=sqrt(ls*ls-b*b);
	 	max=sqrt(ls*ls+b*b);
	 	
	 	printf("%lf %lf\n",min,max);
	 	
	 }
	return 0;
}
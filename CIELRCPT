#include <stdio.h>
#include <math.h>
 
int main(void) 
{
    int n, j, i, p, c=0, t;
    scanf("%d", &n);
    for(j=0; j<n; j
              p=p%t;
           }
        else if(p==0)
        break;
        }
    printf("%d\n", c);
    }
    return 0;
}
#include <stdio.h>
int main() 
{
     int n; long int c, max;
    scanf("%d", &n);
    while(n>0)
    {
        scanf("%ld", &c);
        max=(c/2) +1;
        printf("%d \n", max);
        n--;
    }
	return 0;
}
 
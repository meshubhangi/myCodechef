#include <stdio.h>
#include<math.h>
 
int main(void)
{
    int n, t,i, num;
    scanf("%d", &n);
    for(i=0; i<n; i++)
    {
        scanf("%d", &num);
        t=sqrt(num);
        printf("%d \n", t);
    }
	return 0;
}
 